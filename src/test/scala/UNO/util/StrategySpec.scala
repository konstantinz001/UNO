package UNO.util

import UNO.UnoGame.controller
import org.scalatest.{Matchers, WordSpec}

import scala.List

/*class StrategySpec extends WordSpec with Matchers {
  "strategyEvent1" should{
    val temp = controller.stackCard
    strategyEvent1()
    controller.playerList.size should be(2)
    controller.stackCard.size should be(temp.size - 1)
  }
}
*/


